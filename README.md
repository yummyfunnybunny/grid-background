the overlay does not work with the following effect types:

- mouseover
- click

the overlay prevents cells from listening for mouse events because their z-index places them over the grid effect area
